import UIKit

public extension UIView {
    /// Changes that cause an aspect of a view to be invalid and require an update.
    ///
    /// In iOS 15 this is `UIView.Invalidations`
    enum Invalidation {}
}
